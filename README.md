# Quadratic Equation Solver (Android)

Android-приложение для решения квадратных уравнений вида:

ax² + bx + c = 0

Проект реализован в рамках практической работы по теме обработки событий в Android.

---

## 📱 Функциональность

Приложение позволяет:

- Ввести коэффициенты A, B и C
- Вычислить дискриминант (D)
- Найти корни уравнения (X1, X2)
- Обработать частные случаи:
  - A = 0 (не квадратное уравнение)
  - D < 0 (нет действительных корней)
  - Пустой или некорректный ввод
- Вывести дискриминант через `Toast`
- Отобразить результат в `TextView`
- Дополнительно: вычислить сумму коэффициентов

---

## 🛠 Технологии

- Kotlin
- Android SDK (Min SDK 28)
- AppCompat
- View-based UI (XML Layout)
- Обработка событий через `setOnClickListener`

---

## 🧠 Логика работы

1. Пользователь вводит коэффициенты
2. При нажатии кнопки:
   - Проверяется корректность ввода
   - Строки преобразуются в `Double`
   - Вычисляется дискриминант:
     
     D = b² − 4ac

3. В зависимости от значения D:
   - D > 0 → два корня
   - D = 0 → один корень
   - D < 0 → действительных корней нет

4. Результат форматируется до двух знаков после запятой

---

## 📷 Интерфейс

- 3 поля ввода (EditText)
- Кнопка "Решить"
- Кнопка "Сумма коэффициентов"
- Область вывода результата

---

## 🚀 Запуск проекта

1. Открыть проект в Android Studio
2. Выполнить Sync Gradle
3. Запустить на эмуляторе или реальном устройстве

---

## 📚 Цель проекта

Закрепление навыков:

- работы с обработчиками событий
- получения данных из EditText
- обработки исключений
- использования Toast
- структурирования Android-приложения
